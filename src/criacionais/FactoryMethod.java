package criacionais;
/* 
public class FactoryMethod {    

    //o código não roda, é apenas exempleficar!
    
// Interface que define o Factory Method
interface Animal {
    void makeSound();
}

// Implementação concreta da interface
class Dog implements Animal {
    @Override
    public void makeSound() {
        System.out.println("Woof!");
    }
}

// Outra implementação concreta da interface
class Cat implements Animal {
    @Override
    public void makeSound() {
        System.out.println("Meow!");
    }
}

// Interface para o Factory Method
interface AnimalFactory {
    Animal createAnimal();
}

// Implementação concreta da fábrica para criar Dogs
class DogFactory implements AnimalFactory {
    @Override
    public Animal createAnimal() {
        return new Dog();
    }
}

// Implementação concreta da fábrica para criar Cats
class CatFactory implements AnimalFactory {
    @Override
    public Animal createAnimal() {
        return new Cat();
    }
}

// Classe cliente que usa o Factory Method
public class Client {
    public static void main(String[] args) {
        // Usando a fábrica para criar um Dog
        AnimalFactory dogFactory = new DogFactory();
        Animal dog = dogFactory.createAnimal();
        dog.makeSound();

        // Usando a fábrica para criar um Cat
        AnimalFactory catFactory = new CatFactory();
        Animal cat = catFactory.createAnimal();
        cat.makeSound();
    }
}

}*/
